# Top-level runtime dependencies for the SimHash + LSH benchmark.
# Install with:
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1     # (Windows PowerShell)
#   pip install -r requirements.txt
#
# External (NOT pip): Java 17 JDK (Eclipse Temurin recommended) on PATH
# or via JAVA_HOME — required by PySpark.

pyspark==4.1.2
numpy==2.4.6
pandas==3.0.3
matplotlib==3.10.9
psutil==7.2.2
